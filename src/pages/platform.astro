---
import Layout from '../layouts/Layout.astro'
---

<Layout title="The Complete On-Premise AI Platform" description="AskSLM combines a C++ inference engine, secure model marketplace, and one-click deployment -- all running on your hardware. No cloud. No compromises.">

  <!-- ===== HERO ===== -->
  <section class="relative min-h-[85svh] flex flex-col justify-end pb-16 md:justify-center md:pb-0 overflow-hidden">
    <div class="absolute inset-0 pointer-events-none">
      <div class="absolute top-[15%] left-[10%] w-[50vw] h-[50vw] bg-radial-[at_50%_50%] from-gold/[0.015] to-transparent rounded-full"></div>
      <div class="absolute bottom-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-gold/10 to-transparent"></div>
    </div>

    <div class="relative mx-auto w-full max-w-[90rem] px-6 md:px-10 pt-32 md:pt-0">
      <p class="font-mono text-[11px] text-gold/50 tracking-[0.3em] uppercase mb-10 md:mb-14" data-reveal>
        Platform Overview
      </p>

      <div class="grid md:grid-cols-12 gap-8 md:gap-6">
        <div class="md:col-span-8">
          <h1 class="font-display leading-[0.85] tracking-[-0.04em]" data-reveal data-reveal-delay="1">
            <span class="block text-[clamp(3rem,9vw,8rem)] text-bright">The complete</span>
            <span class="block text-[clamp(3rem,9vw,8rem)] text-bright/25 md:ml-[4vw]">on-premise</span>
            <span class="block text-[clamp(3rem,9vw,8rem)] text-bright">AI platform<span class="text-gold">.</span></span>
          </h1>
        </div>

        <div class="md:col-span-4 md:flex md:flex-col md:justify-end md:pb-4" data-reveal data-reveal-delay="2">
          <p class="text-[15px] text-text leading-relaxed max-w-sm">
            A high-performance inference engine, a secure model marketplace, and production-ready deployment -- all running entirely on your infrastructure. No cloud dependencies. No data exposure.
          </p>
          <div class="mt-8 flex flex-col sm:flex-row md:flex-col gap-3">
            <a href="/contact" class="group inline-flex items-center justify-center gap-2.5 rounded bg-gold px-6 py-3 text-sm font-semibold text-black transition-all duration-200 hover:bg-gold-bright">
              Schedule a Demo
              <svg class="h-4 w-4 transition-transform duration-200 group-hover:translate-x-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M17 8l4 4m0 0l-4 4m4-4H3" /></svg>
            </a>
            <a href="#engine" class="inline-flex items-center justify-center gap-2 rounded border border-border px-6 py-3 text-sm font-medium text-text transition-all duration-200 hover:border-gold/30 hover:text-bright">
              Explore the Architecture
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== PLATFORM PILLARS ===== -->
  <div class="border-t border-border">
    <div class="mx-auto max-w-[90rem] px-6 md:px-10">
      <div class="grid sm:grid-cols-3" data-reveal>
        {[
          { label: 'Inference Engine', anchor: '#engine', detail: 'C++ core, standard hardware' },
          { label: 'Model Marketplace', anchor: '#marketplace', detail: 'Curated, domain-specific models' },
          { label: 'Deployment', anchor: '#deployment', detail: 'Your hardware or our appliance' },
        ].map((pill, i) => (
          <a href={pill.anchor} class="group flex items-center gap-4 border-b sm:border-b-0 sm:border-r border-border last:border-0 py-6 sm:py-8 sm:pr-8 transition-colors duration-300 hover:bg-surface/30" data-reveal-delay={String(i + 1)}>
            <div class="w-6 h-px bg-gold/40 shrink-0 group-hover:bg-gold transition-colors"></div>
            <div>
              <span class="text-sm font-semibold text-bright group-hover:text-gold transition-colors">{pill.label}</span>
              <span class="block text-xs text-muted mt-0.5">{pill.detail}</span>
            </div>
          </a>
        ))}
      </div>
    </div>
  </div>

  <!-- ===== INFERENCE ENGINE ===== -->
  <section id="engine" class="border-t border-border py-28 md:py-40">
    <div class="mx-auto max-w-[90rem] px-6 md:px-10">
      <!-- Section header -->
      <div class="grid md:grid-cols-12 gap-6 mb-20" data-reveal>
        <div class="md:col-span-7">
          <p class="font-mono text-[10px] text-gold/50 tracking-[0.3em] uppercase mb-6">Inference Engine</p>
          <h2 class="font-display text-4xl md:text-5xl lg:text-6xl tracking-[-0.02em] leading-[1.05]">Like an operating system<br />for AI<span class="text-gold">.</span></h2>
        </div>
        <div class="md:col-span-5 md:flex md:items-end">
          <p class="text-text text-[15px] leading-relaxed max-w-md">
            At the core of the AskSLM platform is a C++ inference engine built for maximum performance on standard enterprise hardware. Run multiple AI models simultaneously, with real-time responses and zero cloud dependency.
          </p>
        </div>
      </div>

      <!-- Secondary paragraph -->
      <div class="max-w-2xl mb-20" data-reveal>
        <p class="text-text leading-relaxed">
          No expensive GPU clusters. No per-token pricing. No cold starts. Just production-grade AI running on the servers already in your data center.
        </p>
      </div>

      <!-- Feature grid -->
      <div class="grid md:grid-cols-2 gap-x-16 lg:gap-x-24 gap-y-12 md:gap-y-16 mb-24">
        {[
          { num: '01', title: 'Standard hardware', desc: 'Runs on enterprise servers, workstations, and high-end machines you already own. No GPU clusters required.' },
          { num: '02', title: 'Multi-model concurrency', desc: 'Keep specialized models -- legal, medical, compliance, analytics -- active in parallel on a single deployment.' },
          { num: '03', title: 'Real-time performance', desc: 'Local execution means zero network latency. Models stay warm in memory and respond instantly.' },
          { num: '04', title: 'C++ from the ground up', desc: 'Purpose-built for inference performance. Not a wrapper around open-source frameworks. Every line written for speed.' },
        ].map((feature, i) => (
          <div data-reveal data-reveal-delay={String(i + 1)}>
            <span class="font-mono text-[11px] text-gold/25 tracking-[0.15em]">{feature.num}</span>
            <h3 class="font-display text-xl md:text-2xl text-bright mt-2 mb-3 tracking-tight">{feature.title}</h3>
            <p class="text-text text-sm leading-relaxed">{feature.desc}</p>
          </div>
        ))}
      </div>

      <!-- Engine metrics -->
      <div class="border-t border-border pt-16" data-reveal>
        <div class="grid md:grid-cols-12 gap-6 items-start mb-12">
          <div class="md:col-span-5">
            <h3 class="font-display text-2xl md:text-3xl text-bright tracking-tight">Engine capabilities<span class="text-gold">.</span></h3>
          </div>
        </div>

        <div class="space-y-0">
          {[
            { metric: 'Concurrent models', value: 'Multiple', detail: 'Run legal, medical, compliance models simultaneously' },
            { metric: 'Hardware requirement', value: 'Standard', detail: 'Enterprise servers and workstations -- no GPU clusters' },
            { metric: 'Network latency', value: '0ms', detail: 'Local execution eliminates round-trip delay' },
            { metric: 'Cold start time', value: 'None', detail: 'Models stay warm in memory for instant response' },
            { metric: 'Cost vs cloud APIs', value: '5-20x less', detail: 'Fixed costs, no per-token metering' },
          ].map((row, i) => (
            <div class="grid grid-cols-12 gap-4 items-baseline border-t border-border py-5" data-reveal data-reveal-delay={String(i + 1)}>
              <div class="col-span-5 md:col-span-4">
                <p class="text-sm text-text">{row.metric}</p>
              </div>
              <div class="col-span-4 md:col-span-5">
                <p class="text-xs text-muted">{row.detail}</p>
              </div>
              <div class="col-span-3 text-right">
                <span class="font-mono text-sm font-semibold text-gold">{row.value}</span>
              </div>
            </div>
          ))}
          <div class="border-t border-border"></div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== MODEL MARKETPLACE ===== -->
  <section id="marketplace" class="border-t border-border py-28 md:py-40">
    <div class="mx-auto max-w-[90rem] px-6 md:px-10">
      <!-- Section header -->
      <div class="grid md:grid-cols-12 gap-6 mb-20" data-reveal>
        <div class="md:col-span-7">
          <p class="font-mono text-[10px] text-gold/50 tracking-[0.3em] uppercase mb-6">Model Marketplace</p>
          <h2 class="font-display text-4xl md:text-5xl lg:text-6xl tracking-[-0.02em] leading-[1.05]">An app store for<br />domain-specific AI<span class="text-gold">.</span></h2>
        </div>
        <div class="md:col-span-5 md:flex md:items-end">
          <p class="text-text text-[15px] leading-relaxed max-w-md">
            The SLM Marketplace is a curated library of AI models built by verified vendors for regulated industries. Legal research, clinical decision support, compliance analysis, financial risk assessment -- browse, deploy, and run specialized models in minutes.
          </p>
        </div>
      </div>

      <!-- Architecture features -->
      <div class="grid md:grid-cols-3 gap-x-16 lg:gap-x-24 gap-y-12 mb-24">
        {[
          {
            num: '01',
            title: 'Trust broker architecture',
            desc: 'The marketplace coordinates access without ever seeing your data or the vendor\'s model internals. A cryptographic trust layer sits between both parties.',
          },
          {
            num: '02',
            title: 'Two-way protection',
            desc: 'Your data remains private. Vendor model IP stays secure. End-to-end encrypted connections between your engine and vendor model environments.',
          },
          {
            num: '03',
            title: 'Encrypted pipelines',
            desc: 'Models are deployed through secure, encrypted channels. No raw data or model weights are transferred during operation.',
          },
        ].map((feature, i) => (
          <div data-reveal data-reveal-delay={String(i + 1)}>
            <span class="font-mono text-[11px] text-gold/25 tracking-[0.15em]">{feature.num}</span>
            <h3 class="font-display text-xl md:text-2xl text-bright mt-2 mb-3 tracking-tight">{feature.title}</h3>
            <p class="text-text text-sm leading-relaxed">{feature.desc}</p>
          </div>
        ))}
      </div>

      <!-- Model categories -->
      <div class="border-t border-border pt-16" data-reveal>
        <div class="grid md:grid-cols-12 gap-6 items-start mb-12">
          <div class="md:col-span-5">
            <h3 class="font-display text-2xl md:text-3xl text-bright tracking-tight">Available model categories<span class="text-gold">.</span></h3>
          </div>
          <div class="md:col-span-7 md:flex md:justify-end">
            <p class="text-xs text-muted">All models deployed through encrypted pipelines. Your data never leaves your infrastructure.</p>
          </div>
        </div>

        <div class="space-y-0">
          {[
            { name: 'Legal Research', status: 'Available' },
            { name: 'E-Discovery', status: 'Available' },
            { name: 'Clinical Decision Support', status: 'Available' },
            { name: 'Medical Coding', status: 'Available' },
            { name: 'Compliance & Policy', status: 'Available' },
            { name: 'Financial Risk', status: 'Available' },
            { name: 'KYC/AML Analysis', status: 'Coming soon' },
            { name: 'General Purpose', status: 'Available' },
          ].map((model, i) => (
            <div class="grid grid-cols-12 gap-4 items-center border-t border-border py-4" data-reveal data-reveal-delay={String(Math.min(i + 1, 6))}>
              <div class="col-span-8 md:col-span-6">
                <p class="text-sm text-bright">{model.name}</p>
              </div>
              <div class="col-span-4 md:col-span-6 flex items-center justify-end gap-2">
                <span class={`h-1.5 w-1.5 rounded-full ${model.status === 'Available' ? 'bg-safe' : 'bg-muted'}`}></span>
                <span class="font-mono text-[10px] text-muted tracking-[0.1em] uppercase">{model.status}</span>
              </div>
            </div>
          ))}
          <div class="border-t border-border"></div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== DEPLOYMENT ===== -->
  <section id="deployment" class="border-t border-border py-28 md:py-40">
    <div class="mx-auto max-w-[90rem] px-6 md:px-10">
      <!-- Section header -->
      <div class="grid md:grid-cols-12 gap-6 mb-20" data-reveal>
        <div class="md:col-span-7">
          <p class="font-mono text-[10px] text-gold/50 tracking-[0.3em] uppercase mb-6">Deployment</p>
          <h2 class="font-display text-4xl md:text-5xl lg:text-6xl tracking-[-0.02em] leading-[1.05]">Production-ready in days<span class="text-gold">.</span><br /><span class="text-bright/25">Not quarters.</span></h2>
        </div>
        <div class="md:col-span-5 md:flex md:items-end">
          <p class="text-text text-[15px] leading-relaxed max-w-md">
            Deploy on the hardware you already own or use our pre-configured AI appliance. No ML team required.
          </p>
        </div>
      </div>

      <!-- Two deployment options as editorial rows -->
      <div class="space-y-0">
        <!-- Option A: Your hardware -->
        <div class="border-t border-border py-12 md:py-16" data-reveal>
          <div class="grid md:grid-cols-12 gap-8 items-start">
            <span class="font-mono text-xs text-gold/40 md:col-span-1">01</span>
            <div class="md:col-span-4">
              <h3 class="font-display text-2xl md:text-3xl text-bright tracking-tight">Your existing hardware</h3>
              <p class="font-mono text-[10px] text-muted/60 tracking-[0.1em] mt-2">Deploy on what you already own</p>
            </div>
            <div class="md:col-span-7">
              <p class="text-text text-[15px] leading-relaxed mb-8">
                Turn your current servers, workstations, and data center machines into a private AI cluster. Virtual machine images or direct installations integrate seamlessly with your IT stack.
              </p>
              <div class="grid sm:grid-cols-2 gap-4">
                {[
                  'Runs on modern Mac, Linux, or Windows machines',
                  'Leverages hardware you already own and manage',
                  'Ideal for data centers and secure enterprise networks',
                  'Integrates with existing IT and security infrastructure',
                ].map(item => (
                  <div class="flex items-start gap-3">
                    <div class="w-6 h-px bg-gold/40 mt-[11px] shrink-0"></div>
                    <p class="text-text text-sm leading-relaxed">{item}</p>
                  </div>
                ))}
              </div>
            </div>
          </div>
        </div>

        <!-- Option B: SLM Appliance -->
        <div class="border-t border-border py-12 md:py-16" data-reveal data-reveal-delay="1">
          <div class="grid md:grid-cols-12 gap-8 items-start">
            <span class="font-mono text-xs text-gold/40 md:col-span-1">02</span>
            <div class="md:col-span-4">
              <h3 class="font-display text-2xl md:text-3xl text-bright tracking-tight">SLM Appliance</h3>
              <p class="font-mono text-[10px] text-muted/60 tracking-[0.1em] mt-2">AI in a box -- plug and play</p>
            </div>
            <div class="md:col-span-7">
              <p class="text-text text-[15px] leading-relaxed mb-8">
                A compact hardware device pre-loaded with the inference engine and models. Plug it into your network and start using private AI immediately. Zero provisioning required.
              </p>
              <div class="grid sm:grid-cols-2 gap-4">
                {[
                  'Pre-configured with engine, models, and security policies',
                  'Zero cloud setup or infrastructure provisioning',
                  'Perfect for branch offices, clinics, or remote sites',
                  'Energy-efficient and cost-effective at any scale',
                ].map(item => (
                  <div class="flex items-start gap-3">
                    <div class="w-6 h-px bg-gold/40 mt-[11px] shrink-0"></div>
                    <p class="text-text text-sm leading-relaxed">{item}</p>
                  </div>
                ))}
              </div>
            </div>
          </div>
        </div>
        <div class="border-t border-border"></div>
      </div>

      <!-- On-premise callout -->
      <div class="pt-12 text-center" data-reveal>
        <p class="text-sm text-text">
          In both deployment modes, all AI processing stays on-premise. <span class="text-bright font-medium">No data ever leaves your location.</span>
        </p>
      </div>
    </div>
  </section>

  <!-- ===== ARCHITECTURE DIAGRAM ===== -->
  <section class="border-t border-border py-28 md:py-40">
    <div class="mx-auto max-w-[90rem] px-6 md:px-10">
      <div class="mb-20" data-reveal>
        <p class="font-mono text-[10px] text-gold/50 tracking-[0.3em] uppercase mb-6">Architecture</p>
        <h2 class="font-display text-4xl md:text-5xl tracking-[-0.02em]">System architecture<span class="text-gold">.</span></h2>
        <p class="mt-4 text-text text-[15px] max-w-xl">How the pieces fit together -- entirely within your perimeter.</p>
      </div>

      <div class="border border-border rounded p-8 md:p-12 overflow-x-auto bg-surface/50" data-reveal data-reveal-delay="1">
        <pre class="text-xs sm:text-sm font-mono text-text leading-relaxed whitespace-pre overflow-x-auto"><code>{`
  Your Infrastructure (On-Premise)
  +================================================================+
  |                                                                |
  |   +------------------+      +-----------------------------+    |
  |   |   Your Users     |      |   Your Data Sources         |    |
  |   |  (Browser / API) | ---> |  Documents, DBs, Knowledge  |    |
  |   +------------------+      +-----------------------------+    |
  |           |                            |                       |
  |           v                            v                       |
  |   +----------------------------------------------------+      |
  |   |            AskSLM Platform Layer                    |      |
  |   |                                                    |      |
  |   |   +------------------+   +---------------------+   |      |
  |   |   |  Access Control  |   |  Audit & Logging    |   |      |
  |   |   |  (RBAC)          |   |  (Full Trail)       |   |      |
  |   |   +------------------+   +---------------------+   |      |
  |   |                                                    |      |
  |   |   +--------------------------------------------+   |      |
  |   |   |         C++ Inference Engine                |   |      |
  |   |   |                                            |   |      |
  |   |   |   [Legal]  [Clinical]  [Compliance]  ...   |   |      |
  |   |   |   Model A   Model B    Model C             |   |      |
  |   |   |                                            |   |      |
  |   |   |   Multi-model concurrency on standard HW   |   |      |
  |   |   +--------------------------------------------+   |      |
  |   |                                                    |      |
  |   |   +------------------+   +---------------------+   |      |
  |   |   |  RAG Pipeline    |   |  Training Engine    |   |      |
  |   |   |  (Knowledge      |   |  (No-Code, Domain   |   |      |
  |   |   |   Retrieval)     |   |   Customization)    |   |      |
  |   |   +------------------+   +---------------------+   |      |
  |   +----------------------------------------------------+      |
  |                        |                                       |
  +========================|=======================================+
                           |
                 Encrypted | Trust Broker
                           |
  +========================|=======================================+
  |              SLM Marketplace (Secure Channel)                  |
  |                                                                |
  |   Verified vendors publish models via encrypted pipelines.     |
  |   Your data never leaves. Vendor IP stays protected.           |
  +================================================================+
`}</code></pre>
      </div>

      <!-- Security principles -->
      <div class="grid md:grid-cols-3 gap-x-16 lg:gap-x-24 gap-y-12 mt-16">
        {[
          { num: '01', title: 'Data never leaves', desc: 'Every query, every response, every training job stays within your infrastructure perimeter.' },
          { num: '02', title: 'Encrypted at every layer', desc: 'Hardware-bound encryption, TLS between components, and Trusted Execution Environments protect data at rest and in transit.' },
          { num: '03', title: 'Full audit trail', desc: 'Every model interaction is logged. Every access is recorded. Every decision is traceable for compliance review.' },
        ].map((point, i) => (
          <div data-reveal data-reveal-delay={String(i + 1)}>
            <span class="font-mono text-[11px] text-gold/25 tracking-[0.15em]">{point.num}</span>
            <h3 class="font-display text-xl md:text-2xl text-bright mt-2 mb-3 tracking-tight">{point.title}</h3>
            <p class="text-text text-sm leading-relaxed">{point.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ===== CTA ===== -->
  <section class="relative py-36 md:py-48 overflow-hidden">
    <div class="absolute inset-0 pointer-events-none">
      <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[60vw] h-[60vw] bg-radial-[at_50%_50%] from-gold/[0.025] to-transparent rounded-full"></div>
    </div>
    <div class="absolute top-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-gold/10 to-transparent"></div>

    <div class="relative mx-auto max-w-2xl px-6 text-center" data-reveal>
      <p class="font-mono text-[10px] text-gold/50 tracking-[0.3em] uppercase mb-10">Get Started</p>
      <h2 class="font-display text-5xl md:text-6xl lg:text-7xl tracking-[-0.03em] leading-[0.95]">
        See the platform<br />in action<span class="text-gold">.</span>
      </h2>
      <p class="mt-10 text-text text-lg max-w-md mx-auto leading-relaxed">
        We'll walk you through a live deployment on standard hardware -- tailored to your industry, your data, and your compliance requirements.
      </p>
      <div class="mt-10 flex flex-wrap justify-center gap-4">
        <a href="/contact" class="group inline-flex items-center gap-2.5 rounded bg-gold px-8 py-3.5 text-sm font-semibold text-black transition-all duration-200 hover:bg-gold-bright hover:glow-gold-strong">
          Request a Demo
          <svg class="h-4 w-4 transition-transform duration-200 group-hover:translate-x-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M17 8l4 4m0 0l-4 4m4-4H3" /></svg>
        </a>
        <a href="/security" class="inline-flex items-center gap-2 rounded border border-border px-8 py-3.5 text-sm font-medium text-text transition-all duration-200 hover:border-gold/30 hover:text-bright">
          Security Architecture
        </a>
      </div>
      <p class="mt-10 text-xs text-muted">No commitment. No data shared. 30-minute call.</p>
    </div>
  </section>

</Layout>

<script>
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add('revealed');
          observer.unobserve(entry.target);
        }
      });
    },
    { threshold: 0.1, rootMargin: '0px 0px -40px 0px' }
  );
  document.querySelectorAll('[data-reveal]').forEach((el) => observer.observe(el));
</script>
