---
import Layout from '../layouts/Layout.astro'
---

<Layout title="The Complete On-Premise AI Platform" description="AskSLM combines a C++ inference engine, secure model marketplace, and one-click deployment -- all running on your hardware. No cloud. No compromises.">

  <!-- ===== HERO ===== -->
  <section class="hero-gradient relative overflow-hidden pt-32 pb-20 md:pt-44 md:pb-32">
    <div class="absolute inset-0 opacity-[0.03]" aria-hidden="true">
      <svg class="h-full w-full" xmlns="http://www.w3.org/2000/svg">
        <defs>
          <pattern id="hex-platform" width="56" height="100" patternUnits="userSpaceOnUse" patternTransform="scale(2)">
            <path d="M28 66L0 50V18L28 2l28 16v32L28 66z" fill="none" stroke="currentColor" stroke-width="0.5" class="text-teal" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#hex-platform)" />
      </svg>
    </div>

    <div class="relative mx-auto max-w-7xl px-6">
      <div class="max-w-3xl">
        <div class="inline-flex items-center gap-2 rounded-full border border-navy-700 bg-navy-900/80 px-4 py-1.5 text-xs text-slate-400 mb-8">
          <span class="h-1.5 w-1.5 rounded-full bg-teal animate-pulse"></span>
          Platform overview
        </div>

        <h1 class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold tracking-tight leading-[1.08]">
          The complete
          <br />
          <span class="text-gradient">on-premise AI platform.</span>
        </h1>

        <p class="mt-6 text-lg md:text-xl text-slate-400 max-w-2xl leading-relaxed">
          A high-performance inference engine, a secure model marketplace, and production-ready deployment -- all running entirely on your infrastructure. No cloud dependencies. No data exposure.
        </p>

        <div class="mt-10 flex flex-wrap gap-4">
          <a href="/contact" class="inline-flex items-center gap-2 rounded-lg bg-teal px-6 py-3 text-base font-semibold text-navy-950 transition-all duration-200 hover:bg-teal-dim hover:glow-teal">
            Schedule a Demo
            <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" /></svg>
          </a>
          <a href="#engine" class="inline-flex items-center gap-2 rounded-lg border border-navy-700 px-6 py-3 text-base font-medium text-slate-300 transition-all duration-200 hover:border-slate-600 hover:text-ice">
            Explore the Architecture
          </a>
        </div>
      </div>

      <!-- Platform pillars -->
      <div class="mt-16 grid sm:grid-cols-3 gap-4">
        {[
          { label: 'Inference Engine', anchor: '#engine', detail: 'C++ core, standard hardware' },
          { label: 'Model Marketplace', anchor: '#marketplace', detail: 'Curated, domain-specific models' },
          { label: 'Deployment', anchor: '#deployment', detail: 'Your hardware or our appliance' },
        ].map(pill => (
          <a href={pill.anchor} class="group flex items-center gap-3 rounded-xl border border-navy-700 bg-navy-900/50 px-5 py-4 transition-all duration-300 hover:border-teal/30 hover:glow-teal">
            <span class="h-2 w-2 rounded-full bg-teal group-hover:bg-teal-dim transition-colors"></span>
            <div>
              <span class="text-sm font-semibold text-ice">{pill.label}</span>
              <span class="block text-xs text-slate-400">{pill.detail}</span>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- ===== INFERENCE ENGINE ===== -->
  <section id="engine" class="section-border py-20 md:py-28">
    <div class="mx-auto max-w-7xl px-6">
      <div class="grid lg:grid-cols-2 gap-16 items-start">
        <div>
          <div class="inline-flex items-center gap-2 rounded-full border border-navy-700 bg-navy-900/80 px-4 py-1.5 text-xs text-slate-400 mb-6">
            <span class="h-1.5 w-1.5 rounded-full bg-teal"></span>
            Inference Engine
          </div>

          <h2 class="text-3xl md:text-4xl font-bold tracking-tight">
            Like an operating system
            <br />
            <span class="text-gradient">for AI.</span>
          </h2>

          <p class="mt-6 text-lg text-slate-400 leading-relaxed">
            At the core of the AskSLM platform is a C++ inference engine built for maximum performance on standard enterprise hardware. Run multiple AI models simultaneously, with real-time responses and zero cloud dependency.
          </p>

          <p class="mt-4 text-slate-400 leading-relaxed">
            No expensive GPU clusters. No per-token pricing. No cold starts. Just production-grade AI running on the servers already in your data center.
          </p>

          <div class="mt-10 grid sm:grid-cols-2 gap-6">
            {[
              { title: 'Standard hardware', desc: 'Runs on enterprise servers, workstations, and high-end machines you already own. No GPU clusters required.' },
              { title: 'Multi-model concurrency', desc: 'Keep specialized models -- legal, medical, compliance, analytics -- active in parallel on a single deployment.' },
              { title: 'Real-time performance', desc: 'Local execution means zero network latency. Models stay warm in memory and respond instantly.' },
              { title: 'C++ from the ground up', desc: 'Purpose-built for inference performance. Not a wrapper around open-source frameworks. Every line written for speed.' },
            ].map(feature => (
              <div>
                <h3 class="text-sm font-semibold text-ice mb-1.5">{feature.title}</h3>
                <p class="text-sm text-slate-400 leading-relaxed">{feature.desc}</p>
              </div>
            ))}
          </div>
        </div>

        <!-- Engine metrics card -->
        <div class="card-surface rounded-2xl p-8 lg:mt-12">
          <h3 class="text-lg font-semibold text-ice mb-8">Engine capabilities</h3>
          <div class="space-y-6">
            {[
              { metric: 'Concurrent models', value: 'Multiple', detail: 'Run legal, medical, compliance models simultaneously' },
              { metric: 'Hardware requirement', value: 'Standard', detail: 'Enterprise servers and workstations -- no GPU clusters' },
              { metric: 'Network latency', value: '0ms', detail: 'Local execution eliminates round-trip delay' },
              { metric: 'Cold start time', value: 'None', detail: 'Models stay warm in memory for instant response' },
              { metric: 'Cost vs cloud APIs', value: '5-20x less', detail: 'Fixed costs, no per-token metering' },
            ].map(row => (
              <div class="flex items-start gap-4 pb-6 border-b border-navy-800 last:border-0 last:pb-0">
                <div class="flex-1">
                  <p class="text-sm text-slate-400">{row.metric}</p>
                  <p class="text-xs text-slate-600 mt-0.5">{row.detail}</p>
                </div>
                <span class="text-lg font-semibold text-teal font-mono whitespace-nowrap">{row.value}</span>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== MODEL MARKETPLACE ===== -->
  <section id="marketplace" class="section-border py-20 md:py-28">
    <div class="mx-auto max-w-7xl px-6">
      <div class="grid lg:grid-cols-2 gap-16 items-start">
        <!-- Marketplace models card (left on desktop) -->
        <div class="card-surface rounded-2xl p-8 order-2 lg:order-1 lg:mt-12">
          <h3 class="text-lg font-semibold text-ice mb-6">Available model categories</h3>
          <div class="grid grid-cols-2 gap-4">
            {[
              { name: 'Legal Research', status: 'Available' },
              { name: 'E-Discovery', status: 'Available' },
              { name: 'Clinical Decision Support', status: 'Available' },
              { name: 'Medical Coding', status: 'Available' },
              { name: 'Compliance & Policy', status: 'Available' },
              { name: 'Financial Risk', status: 'Available' },
              { name: 'KYC/AML Analysis', status: 'Coming soon' },
              { name: 'General Purpose', status: 'Available' },
            ].map(model => (
              <div class="rounded-lg border border-navy-800 bg-navy-950/50 p-3">
                <p class="text-sm font-medium text-ice">{model.name}</p>
                <div class="flex items-center gap-1.5 mt-1.5">
                  <span class={`h-1.5 w-1.5 rounded-full ${model.status === 'Available' ? 'bg-emerald' : 'bg-slate-600'}`}></span>
                  <span class="text-xs text-slate-400">{model.status}</span>
                </div>
              </div>
            ))}
          </div>
          <p class="text-xs text-slate-600 mt-6">All models deployed through encrypted pipelines. Your data never leaves your infrastructure.</p>
        </div>

        <!-- Marketplace description (right on desktop) -->
        <div class="order-1 lg:order-2">
          <div class="inline-flex items-center gap-2 rounded-full border border-navy-700 bg-navy-900/80 px-4 py-1.5 text-xs text-slate-400 mb-6">
            <span class="h-1.5 w-1.5 rounded-full bg-teal"></span>
            Model Marketplace
          </div>

          <h2 class="text-3xl md:text-4xl font-bold tracking-tight">
            An app store for
            <br />
            <span class="text-gradient">domain-specific AI.</span>
          </h2>

          <p class="mt-6 text-lg text-slate-400 leading-relaxed">
            The SLM Marketplace is a curated library of AI models built by verified vendors for regulated industries. Legal research, clinical decision support, compliance analysis, financial risk assessment -- browse, deploy, and run specialized models in minutes.
          </p>

          <div class="mt-10 space-y-8">
            {[
              {
                title: 'Trust broker architecture',
                desc: 'The marketplace coordinates access without ever seeing your data or the vendor\'s model internals. A cryptographic trust layer sits between both parties.',
                icon: `<path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 12.75L11.25 15 15 9.75m-3-7.036A11.959 11.959 0 013.598 6 11.99 11.99 0 003 9.749c0 5.592 3.824 10.29 9 11.623 5.176-1.332 9-6.03 9-11.622 0-1.31-.21-2.571-.598-3.751h-.152c-3.196 0-6.1-1.248-8.25-3.285z" />`,
              },
              {
                title: 'Two-way protection',
                desc: 'Your data remains private. Vendor model IP stays secure. End-to-end encrypted connections between your engine and vendor model environments.',
                icon: `<path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M16.5 10.5V6.75a4.5 4.5 0 10-9 0v3.75m-.75 11.25h10.5a2.25 2.25 0 002.25-2.25v-6.75a2.25 2.25 0 00-2.25-2.25H6.75a2.25 2.25 0 00-2.25 2.25v6.75a2.25 2.25 0 002.25 2.25z" />`,
              },
              {
                title: 'Encrypted pipelines',
                desc: 'Models are deployed through secure, encrypted channels. No raw data or model weights are transferred during operation.',
                icon: `<path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M7.5 21L3 16.5m0 0L7.5 12M3 16.5h13.5m0-13.5L21 7.5m0 0L16.5 12M21 7.5H7.5" />`,
              },
            ].map(feature => (
              <div class="flex gap-4">
                <div class="h-10 w-10 flex-shrink-0 rounded-lg bg-teal/10 flex items-center justify-center">
                  <svg class="h-5 w-5 text-teal" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <Fragment set:html={feature.icon} />
                  </svg>
                </div>
                <div>
                  <h3 class="text-base font-semibold text-ice mb-1">{feature.title}</h3>
                  <p class="text-sm text-slate-400 leading-relaxed">{feature.desc}</p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== DEPLOYMENT ===== -->
  <section id="deployment" class="section-border py-20 md:py-28">
    <div class="mx-auto max-w-7xl px-6">
      <div class="text-center max-w-2xl mx-auto mb-16">
        <div class="inline-flex items-center gap-2 rounded-full border border-navy-700 bg-navy-900/80 px-4 py-1.5 text-xs text-slate-400 mb-6">
          <span class="h-1.5 w-1.5 rounded-full bg-teal"></span>
          Deployment
        </div>

        <h2 class="text-3xl md:text-4xl font-bold tracking-tight">
          Production-ready in days.
          <br />
          <span class="text-gradient">Not quarters.</span>
        </h2>

        <p class="mt-6 text-lg text-slate-400">
          Deploy on the hardware you already own or use our pre-configured AI appliance. No ML team required.
        </p>
      </div>

      <div class="grid md:grid-cols-2 gap-8">
        <!-- Option A: Your hardware -->
        <div class="card-surface rounded-2xl p-8 transition-all duration-300 hover:border-teal/30">
          <div class="flex items-center gap-3 mb-6">
            <div class="h-12 w-12 rounded-xl bg-teal/10 flex items-center justify-center">
              <svg class="h-6 w-6 text-teal" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M5 12h14M5 12a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v4a2 2 0 01-2 2M5 12a2 2 0 00-2 2v4a2 2 0 002 2h14a2 2 0 002-2v-4a2 2 0 00-2-2m-2-4h.01M17 16h.01" />
              </svg>
            </div>
            <div>
              <h3 class="text-xl font-semibold text-ice">Your existing hardware</h3>
              <p class="text-sm text-slate-400">Deploy on what you already own</p>
            </div>
          </div>

          <p class="text-sm text-slate-400 leading-relaxed mb-6">
            Turn your current servers, workstations, and data center machines into a private AI cluster. Virtual machine images or direct installations integrate seamlessly with your IT stack.
          </p>

          <ul class="space-y-3">
            {[
              'Runs on modern Mac, Linux, or Windows machines',
              'Leverages hardware you already own and manage',
              'Ideal for data centers and secure enterprise networks',
              'Integrates with existing IT and security infrastructure',
            ].map(item => (
              <li class="flex items-start gap-2.5 text-sm text-slate-300">
                <svg class="h-5 w-5 text-teal flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                </svg>
                {item}
              </li>
            ))}
          </ul>
        </div>

        <!-- Option B: SLM Appliance -->
        <div class="card-surface rounded-2xl p-8 transition-all duration-300 hover:border-teal/30">
          <div class="flex items-center gap-3 mb-6">
            <div class="h-12 w-12 rounded-xl bg-indigo/10 flex items-center justify-center">
              <svg class="h-6 w-6 text-indigo" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M21 7.5l-9-5.25L3 7.5m18 0l-9 5.25m9-5.25v9l-9 5.25M3 7.5l9 5.25M3 7.5v9l9 5.25m0-9v9" />
              </svg>
            </div>
            <div>
              <h3 class="text-xl font-semibold text-ice">SLM Appliance</h3>
              <p class="text-sm text-slate-400">AI in a box -- plug and play</p>
            </div>
          </div>

          <p class="text-sm text-slate-400 leading-relaxed mb-6">
            A compact hardware device pre-loaded with the inference engine and models. Plug it into your network and start using private AI immediately. Zero provisioning required.
          </p>

          <ul class="space-y-3">
            {[
              'Pre-configured with engine, models, and security policies',
              'Zero cloud setup or infrastructure provisioning',
              'Perfect for branch offices, clinics, or remote sites',
              'Energy-efficient and cost-effective at any scale',
            ].map(item => (
              <li class="flex items-start gap-2.5 text-sm text-slate-300">
                <svg class="h-5 w-5 text-indigo flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                </svg>
                {item}
              </li>
            ))}
          </ul>
        </div>
      </div>

      <div class="mt-8 card-surface rounded-xl p-6 text-center">
        <p class="text-sm text-slate-400">
          In both deployment modes, all AI processing stays on-premise. <span class="text-ice font-medium">No data ever leaves your location.</span>
        </p>
      </div>
    </div>
  </section>

  <!-- ===== ARCHITECTURE DIAGRAM ===== -->
  <section class="section-border py-20 md:py-28">
    <div class="mx-auto max-w-7xl px-6">
      <div class="text-center max-w-2xl mx-auto mb-16">
        <h2 class="text-3xl md:text-4xl font-bold tracking-tight">System architecture</h2>
        <p class="mt-4 text-lg text-slate-400">How the pieces fit together -- entirely within your perimeter.</p>
      </div>

      <div class="card-surface rounded-2xl p-8 md:p-12 overflow-x-auto">
        <pre class="text-xs sm:text-sm font-mono text-slate-300 leading-relaxed whitespace-pre overflow-x-auto"><code>{`
  Your Infrastructure (On-Premise)
  +================================================================+
  |                                                                |
  |   +------------------+      +-----------------------------+    |
  |   |   Your Users     |      |   Your Data Sources         |    |
  |   |  (Browser / API) | ---> |  Documents, DBs, Knowledge  |    |
  |   +------------------+      +-----------------------------+    |
  |           |                            |                       |
  |           v                            v                       |
  |   +----------------------------------------------------+      |
  |   |            AskSLM Platform Layer                    |      |
  |   |                                                    |      |
  |   |   +------------------+   +---------------------+   |      |
  |   |   |  Access Control  |   |  Audit & Logging    |   |      |
  |   |   |  (RBAC)          |   |  (Full Trail)       |   |      |
  |   |   +------------------+   +---------------------+   |      |
  |   |                                                    |      |
  |   |   +--------------------------------------------+   |      |
  |   |   |         C++ Inference Engine                |   |      |
  |   |   |                                            |   |      |
  |   |   |   [Legal]  [Clinical]  [Compliance]  ...   |   |      |
  |   |   |   Model A   Model B    Model C             |   |      |
  |   |   |                                            |   |      |
  |   |   |   Multi-model concurrency on standard HW   |   |      |
  |   |   +--------------------------------------------+   |      |
  |   |                                                    |      |
  |   |   +------------------+   +---------------------+   |      |
  |   |   |  RAG Pipeline    |   |  Training Engine    |   |      |
  |   |   |  (Knowledge      |   |  (No-Code, Domain   |   |      |
  |   |   |   Retrieval)     |   |   Customization)    |   |      |
  |   |   +------------------+   +---------------------+   |      |
  |   +----------------------------------------------------+      |
  |                        |                                       |
  +========================|=======================================+
                           |
                 Encrypted | Trust Broker
                           |
  +========================|=======================================+
  |              SLM Marketplace (Secure Channel)                  |
  |                                                                |
  |   Verified vendors publish models via encrypted pipelines.     |
  |   Your data never leaves. Vendor IP stays protected.           |
  +================================================================+
`}</code></pre>
      </div>

      <div class="mt-8 grid sm:grid-cols-3 gap-6">
        {[
          { title: 'Data never leaves', desc: 'Every query, every response, every training job stays within your infrastructure perimeter.' },
          { title: 'Encrypted at every layer', desc: 'Hardware-bound encryption, TLS between components, and Trusted Execution Environments protect data at rest and in transit.' },
          { title: 'Full audit trail', desc: 'Every model interaction is logged. Every access is recorded. Every decision is traceable for compliance review.' },
        ].map(point => (
          <div class="card-surface rounded-xl p-6">
            <h3 class="text-sm font-semibold text-ice mb-2">{point.title}</h3>
            <p class="text-sm text-slate-400 leading-relaxed">{point.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ===== CTA ===== -->
  <section class="relative py-20 md:py-28 overflow-hidden">
    <div class="absolute inset-0 bg-gradient-to-b from-navy-950 via-navy-900 to-navy-950"></div>
    <div class="absolute inset-0 opacity-[0.04]" aria-hidden="true">
      <svg class="h-full w-full" xmlns="http://www.w3.org/2000/svg">
        <defs>
          <pattern id="hex-platform-cta" width="56" height="100" patternUnits="userSpaceOnUse" patternTransform="scale(1.5)">
            <path d="M28 66L0 50V18L28 2l28 16v32L28 66z" fill="none" stroke="currentColor" stroke-width="0.5" class="text-teal" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#hex-platform-cta)" />
      </svg>
    </div>

    <div class="relative mx-auto max-w-3xl px-6 text-center">
      <h2 class="text-3xl md:text-4xl font-bold tracking-tight">See the platform in action</h2>
      <p class="mt-6 text-lg text-slate-400 max-w-xl mx-auto">
        We'll walk you through a live deployment on standard hardware -- tailored to your industry, your data, and your compliance requirements.
      </p>
      <div class="mt-10 flex flex-wrap justify-center gap-4">
        <a href="/contact" class="inline-flex items-center gap-2 rounded-lg bg-teal px-8 py-3.5 text-base font-semibold text-navy-950 transition-all duration-200 hover:bg-teal-dim hover:glow-teal-strong">
          Request a Demo
          <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" /></svg>
        </a>
        <a href="/security" class="inline-flex items-center gap-2 rounded-lg border border-navy-700 px-8 py-3.5 text-base font-medium text-slate-300 hover:border-slate-600 hover:text-ice transition-all duration-200">
          Security Architecture
        </a>
      </div>
      <p class="mt-6 text-sm text-slate-600">No commitment. No data shared. 30-minute call.</p>
    </div>
  </section>

</Layout>
