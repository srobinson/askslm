---
import Layout from '../../layouts/Layout.astro'

const painPoints = [
  {
    num: '01',
    title: 'Financial data exposure',
    desc: 'Every cloud AI request transmits sensitive financial records, customer data, and proprietary models to third-party infrastructure. One breach puts your institution and your customers at risk.',
  },
  {
    num: '02',
    title: 'Regulatory requirements',
    desc: 'OCC, SEC, FINRA, and state regulators require demonstrable data governance. Cloud AI creates audit gaps that examiners will not accept and compliance teams cannot close.',
  },
  {
    num: '03',
    title: 'Unpredictable API costs',
    desc: 'Per-token pricing from cloud AI providers scales linearly with usage. At institutional volume, inference costs become prohibitively expensive with no path to predictable budgeting.',
  },
]

const useCases = [
  {
    title: 'KYC/AML automation',
    desc: 'Automate document intake and review for Know Your Customer and Anti-Money Laundering workflows. Flag irregular transactions, verify identity documents, and accelerate onboarding -- all within your controlled environment.',
  },
  {
    title: 'Risk analysis',
    desc: 'Review lengthy financial filings, risk disclosures, and portfolio reports. AI models flag anomalies, summarize exposure, and surface risk signals that analysts can act on immediately.',
  },
  {
    title: 'Compliance reporting',
    desc: 'Assist compliance teams with evolving regulations and policies. Generate regulatory filings, monitor for policy violations, and maintain audit-ready documentation automatically.',
  },
  {
    title: 'Relationship manager copilots',
    desc: 'Power secure internal copilots for relationship managers. Summarize client portfolios, prepare meeting briefs, and surface relevant product recommendations -- all from on-premise data.',
  },
]
---

<Layout title="AI for Financial Services" description="On-premise AI for banks, insurers, and financial institutions. All data stays behind your firewall.">

  <!-- ===== HERO ===== -->
  <section class="relative min-h-[70svh] flex flex-col justify-end pb-16 md:justify-center md:pb-0 overflow-hidden">
    <div class="absolute inset-0 pointer-events-none">
      <div class="absolute top-[10%] left-[5%] w-[50vw] h-[50vw] bg-radial-[at_50%_50%] from-gold/[0.015] to-transparent rounded-full"></div>
      <div class="absolute bottom-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-gold/10 to-transparent"></div>
    </div>

    <div class="relative mx-auto w-full max-w-[90rem] px-6 md:px-10 pt-32 md:pt-0">
      <div class="mb-10 md:mb-14" data-reveal>
        <a href="/solutions" class="font-mono text-[11px] text-text/50 tracking-[0.2em] uppercase hover:text-gold/60 transition-colors inline-flex items-center gap-2">
          <svg class="h-3.5 w-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" /></svg>
          Solutions
        </a>
        <span class="font-mono text-[11px] text-text/20 tracking-[0.2em] uppercase mx-3">/</span>
        <span class="font-mono text-[11px] text-gold/50 tracking-[0.3em] uppercase">Financial Services</span>
      </div>

      <div class="grid md:grid-cols-12 gap-8 md:gap-6">
        <div class="md:col-span-8">
          <h1 class="font-display leading-[0.85] tracking-[-0.04em]" data-reveal data-reveal-delay="1">
            <span class="block text-[clamp(3rem,9vw,8rem)] text-bright">AI for banks</span>
            <span class="block text-[clamp(3rem,9vw,8rem)] text-bright">that can't afford</span>
            <span class="block text-[clamp(3rem,9vw,8rem)] text-bright">a data breach<span class="text-gold">.</span></span>
          </h1>
        </div>

        <div class="md:col-span-4 md:flex md:flex-col md:justify-end md:pb-4" data-reveal data-reveal-delay="2">
          <p class="text-[15px] text-text leading-relaxed max-w-sm">
            Automate KYC/AML review, generate risk analyses, and power internal copilots for relationship managers -- all running behind your firewall, on your hardware, with no third-party data exposure.
          </p>
          <div class="mt-8 flex flex-col sm:flex-row md:flex-col gap-3">
            <a href="/contact" class="group inline-flex items-center justify-center gap-2.5 rounded bg-gold px-6 py-3 text-sm font-semibold text-black transition-all duration-200 hover:bg-gold-bright">
              Request a Financial Services Demo
              <svg class="h-4 w-4 transition-transform duration-200 group-hover:translate-x-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M17 8l4 4m0 0l-4 4m4-4H3" /></svg>
            </a>
            <a href="/platform" class="inline-flex items-center justify-center gap-2 rounded border border-border px-6 py-3 text-sm font-medium text-text transition-all duration-200 hover:border-gold/30 hover:text-bright">
              See the Platform
            </a>
          </div>
        </div>
      </div>

      <!-- Compliance badges -->
      <div class="mt-16 flex flex-wrap gap-6" data-reveal data-reveal-delay="3">
        {['SOC 2 Aligned', 'Zero Cloud Usage', 'Full Audit Trails', 'Predictable Costs'].map(text => (
          <div class="flex items-center gap-3">
            <div class="w-5 h-px bg-gold/30 shrink-0"></div>
            <span class="font-mono text-[10px] text-text/60 tracking-[0.2em] uppercase">{text}</span>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ===== PAIN POINTS ===== -->
  <section class="relative border-y border-border py-28 md:py-40 overflow-hidden">
    <div class="absolute inset-0 bg-radial-[at_70%_30%] from-danger/[0.015] to-transparent pointer-events-none"></div>

    <div class="mx-auto max-w-[90rem] px-6 md:px-10">
      <div class="grid md:grid-cols-12 gap-6 mb-20" data-reveal>
        <div class="md:col-span-7">
          <p class="font-mono text-[10px] text-danger/40 tracking-[0.3em] uppercase mb-6">The Problem</p>
          <h2 class="font-display text-4xl md:text-5xl tracking-[-0.02em] leading-[1.05]">The financial services<br />AI dilemma<span class="text-danger/40">.</span></h2>
        </div>
        <div class="md:col-span-5 md:flex md:items-end">
          <p class="text-text text-[15px] leading-relaxed">
            Your regulators will not accept "the AI is in the cloud" as an answer. Financial institutions need AI with the data sovereignty and audit trails that regulators actually require.
          </p>
        </div>
      </div>

      <div class="grid md:grid-cols-3 gap-x-16 lg:gap-x-24 gap-y-12">
        {painPoints.map((point, i) => (
          <div data-reveal data-reveal-delay={String(i + 1)}>
            <span class="font-mono text-[11px] text-danger/25 tracking-[0.15em]">{point.num}</span>
            <h3 class="font-display text-xl md:text-2xl text-bright mt-2 mb-3 tracking-tight">{point.title}</h3>
            <p class="text-text text-sm leading-relaxed">{point.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ===== USE CASES ===== -->
  <section class="py-28 md:py-40">
    <div class="mx-auto max-w-[90rem] px-6 md:px-10">
      <div class="grid md:grid-cols-12 gap-6 mb-20" data-reveal>
        <div class="md:col-span-7">
          <p class="font-mono text-[10px] text-gold/50 tracking-[0.3em] uppercase mb-6">Use Cases</p>
          <h2 class="font-display text-4xl md:text-5xl tracking-[-0.02em] leading-[1.05]">How financial institutions use AskSLM<span class="text-gold">.</span></h2>
        </div>
        <div class="md:col-span-5 md:flex md:items-end">
          <p class="text-text text-[15px] leading-relaxed">
            Purpose-built AI models for banking, insurance, and financial compliance -- deployed entirely within your secure network.
          </p>
        </div>
      </div>

      <div class="space-y-0">
        {useCases.map((uc, i) => (
          <div class="border-t border-border py-10 md:py-12" data-reveal data-reveal-delay={String(i + 1)}>
            <div class="grid md:grid-cols-12 gap-4 md:gap-8 items-start">
              <span class="font-mono text-xs text-gold/40 md:col-span-1">0{i + 1}</span>
              <h3 class="md:col-span-4 font-display text-2xl md:text-3xl text-bright tracking-tight">{uc.title}</h3>
              <p class="md:col-span-7 text-text text-[15px] leading-relaxed">{uc.desc}</p>
            </div>
          </div>
        ))}
        <div class="border-t border-border"></div>
      </div>
    </div>
  </section>

  <!-- ===== SCENARIO ===== -->
  <section class="py-28 md:py-40">
    <div class="mx-auto max-w-[90rem] px-6 md:px-10">
      <div class="grid md:grid-cols-12 gap-12">
        <div class="md:col-span-5" data-reveal>
          <p class="font-mono text-[10px] text-gold/50 tracking-[0.3em] uppercase mb-6">In Practice</p>
          <h2 class="font-display text-4xl md:text-5xl tracking-[-0.02em] leading-[1.05]">What this looks like<span class="text-gold">.</span></h2>
        </div>
        <div class="md:col-span-7" data-reveal data-reveal-delay="1">
          <p class="text-text text-[15px] leading-relaxed mb-10">
            A financial institution deploys AskSLM inside its protected network to support analysts and compliance officers. AI models review lengthy financial filings and risk disclosures, flag irregular transactions or AML/KYC concerns, and enable analysts to query models privately through on-premise execution -- with all sensitive financial data staying within the bank's controlled environment.
          </p>
          <div class="grid sm:grid-cols-2 gap-4">
            {[
              'Reviews financial filings and risk disclosures',
              'Flags irregular transactions and AML/KYC concerns',
              'Analysts query models through on-premise execution',
              'All financial data stays within your environment',
            ].map(outcome => (
              <div class="flex items-start gap-3">
                <div class="w-6 h-px bg-gold/40 mt-[11px] shrink-0"></div>
                <p class="text-text text-sm leading-relaxed">{outcome}</p>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== COST SAVINGS ===== -->
  <section class="border-t border-border py-28 md:py-40">
    <div class="mx-auto max-w-[90rem] px-6 md:px-10">
      <div class="grid md:grid-cols-12 gap-6 mb-20" data-reveal>
        <div class="md:col-span-7">
          <p class="font-mono text-[10px] text-gold/50 tracking-[0.3em] uppercase mb-6">Cost Advantage</p>
          <h2 class="font-display text-4xl md:text-5xl tracking-[-0.02em] leading-[1.05]">Cut inference costs by 5-20x<span class="text-gold">.</span></h2>
        </div>
        <div class="md:col-span-5 md:flex md:items-end">
          <p class="text-text text-[15px] leading-relaxed">
            Stop paying per-token for cloud APIs your compliance team cannot approve. AskSLM runs on hardware you already own with completely predictable, fixed costs.
          </p>
        </div>
      </div>

      <div class="grid grid-cols-3 gap-4" data-reveal>
        {[
          { value: '5-20', suffix: 'x', label: 'Cost reduction vs. cloud AI APIs' },
          { value: 'Zero', suffix: '', label: 'Per-token pricing or usage metering' },
          { value: '100', suffix: '%', label: 'On your hardware, in your vault' },
        ].map((stat, i) => (
          <div class={`${i === 1 ? 'text-center' : i === 2 ? 'text-right' : ''}`} data-reveal-delay={String(i + 1)}>
            <div class="font-display text-[clamp(2.5rem,6vw,5.5rem)] leading-none tracking-tight text-bright">
              {stat.value}<span class="text-gold">{stat.suffix}</span>
            </div>
            <div class="text-[10px] font-mono text-muted tracking-[0.2em] uppercase mt-3">{stat.label}</div>
          </div>
        ))}
      </div>

      <div class="border-t border-border mt-16 pt-8" data-reveal>
        <a href="/security" class="font-mono text-[11px] text-gold/50 tracking-[0.2em] uppercase hover:text-gold transition-colors inline-flex items-center gap-2">
          See full security architecture
          <svg class="h-3.5 w-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" /></svg>
        </a>
      </div>
    </div>
  </section>

  <!-- ===== CTA ===== -->
  <section class="relative py-36 md:py-48 overflow-hidden">
    <div class="absolute inset-0 pointer-events-none">
      <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[60vw] h-[60vw] bg-radial-[at_50%_50%] from-gold/[0.025] to-transparent rounded-full"></div>
    </div>
    <div class="absolute top-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-gold/10 to-transparent"></div>

    <div class="relative mx-auto max-w-2xl px-6 text-center" data-reveal>
      <p class="font-mono text-[10px] text-gold/50 tracking-[0.3em] uppercase mb-10">Get Started</p>
      <h2 class="font-display text-5xl md:text-6xl tracking-[-0.03em] leading-[0.95]">
        AI with the data sovereignty<br />your regulators require<span class="text-gold">.</span>
      </h2>
      <p class="mt-10 text-text text-lg max-w-md mx-auto leading-relaxed">
        See AskSLM running in a financial services environment with your compliance requirements and data governance standards.
      </p>
      <div class="mt-10 flex flex-wrap justify-center gap-4">
        <a href="/contact" class="group inline-flex items-center gap-2.5 rounded bg-gold px-8 py-3.5 text-sm font-semibold text-black transition-all duration-200 hover:bg-gold-bright hover:glow-gold-strong">
          Request a Financial Services Demo
          <svg class="h-4 w-4 transition-transform duration-200 group-hover:translate-x-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M17 8l4 4m0 0l-4 4m4-4H3" /></svg>
        </a>
      </div>
      <p class="mt-10 text-xs text-muted">No commitment. No data shared. 30-minute call.</p>
    </div>
  </section>

</Layout>

<script>
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add('revealed');
          observer.unobserve(entry.target);
        }
      });
    },
    { threshold: 0.1, rootMargin: '0px 0px -40px 0px' }
  );
  document.querySelectorAll('[data-reveal]').forEach((el) => observer.observe(el));
</script>
