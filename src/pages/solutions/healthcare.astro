---
import Layout from '../../layouts/Layout.astro'

const painPoints = [
  {
    num: '01',
    title: 'Patient data in the cloud',
    desc: 'Every cloud AI call sends protected health information to infrastructure you do not own, creating HIPAA exposure your compliance team cannot sign off on.',
  },
  {
    num: '02',
    title: 'AI hallucinations in clinical settings',
    desc: 'General-purpose models trained on the entire internet mix sources, hallucinate references, and cannot trace their outputs to authoritative clinical literature.',
  },
  {
    num: '03',
    title: 'No audit trail for AI decisions',
    desc: 'Cloud AI providers disclaim responsibility for outputs. There is no logging, no traceability, and no way to reconstruct how an AI-assisted clinical decision was made.',
  },
]

const useCases = [
  {
    title: 'Patient history summarization',
    desc: 'Summarize complex patient histories, lab results, and clinical notes before consultations. Clinicians get a comprehensive snapshot in seconds, not minutes of chart review.',
  },
  {
    title: 'Diagnostic support',
    desc: 'Flag potential issues, lab anomalies, and relevant differential diagnoses from peer-reviewed clinical guidelines. Domain-specific models trained on verified medical literature.',
  },
  {
    title: 'Medical coding and billing',
    desc: 'Automate ICD-10, CPT, and HCPCS coding workflows. Reduce coding errors, accelerate billing cycles, and free up clinical staff for patient care.',
  },
  {
    title: 'Clinical decision support',
    desc: 'Query AI models through secure, encrypted on-premise connections for evidence-based guidance. Every interaction is logged with full audit trails for compliance review.',
  },
]
---

<Layout title="AI for Healthcare" description="HIPAA-compliant AI that runs entirely on your hospital network. Patient data never leaves your infrastructure.">

  <!-- ===== HERO ===== -->
  <section class="relative min-h-[70svh] flex flex-col justify-end pb-16 md:justify-center md:pb-0 overflow-hidden">
    <div class="absolute inset-0 pointer-events-none">
      <div class="absolute top-[10%] left-[5%] w-[50vw] h-[50vw] bg-radial-[at_50%_50%] from-gold/[0.015] to-transparent rounded-full"></div>
      <div class="absolute bottom-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-gold/10 to-transparent"></div>
    </div>

    <div class="relative mx-auto w-full max-w-[90rem] px-6 md:px-10 pt-32 md:pt-0">
      <div class="mb-10 md:mb-14" data-reveal>
        <a href="/solutions" class="font-mono text-[11px] text-text/50 tracking-[0.2em] uppercase hover:text-gold/60 transition-colors inline-flex items-center gap-2">
          <svg class="h-3.5 w-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" /></svg>
          Solutions
        </a>
        <span class="font-mono text-[11px] text-text/20 tracking-[0.2em] uppercase mx-3">/</span>
        <span class="font-mono text-[11px] text-gold/50 tracking-[0.3em] uppercase">Healthcare</span>
      </div>

      <div class="grid md:grid-cols-12 gap-8 md:gap-6">
        <div class="md:col-span-8">
          <h1 class="font-display leading-[0.85] tracking-[-0.04em]" data-reveal data-reveal-delay="1">
            <span class="block text-[clamp(3rem,9vw,8rem)] text-bright">HIPAA-compliant AI</span>
            <span class="block text-[clamp(3rem,9vw,8rem)] text-bright">that never touches</span>
            <span class="block text-[clamp(3rem,9vw,8rem)] text-bright">the cloud<span class="text-gold">.</span></span>
          </h1>
        </div>

        <div class="md:col-span-4 md:flex md:flex-col md:justify-end md:pb-4" data-reveal data-reveal-delay="2">
          <p class="text-[15px] text-text leading-relaxed max-w-sm">
            Summarize patient histories. Assist with diagnostic decisions. Automate coding and billing. All running on your hospital's own servers, with full audit trails and zero external data exposure.
          </p>
          <div class="mt-8 flex flex-col sm:flex-row md:flex-col gap-3">
            <a href="/contact" class="group inline-flex items-center justify-center gap-2.5 rounded bg-gold px-6 py-3 text-sm font-semibold text-black transition-all duration-200 hover:bg-gold-bright">
              Request a Healthcare Demo
              <svg class="h-4 w-4 transition-transform duration-200 group-hover:translate-x-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M17 8l4 4m0 0l-4 4m4-4H3" /></svg>
            </a>
            <a href="/platform" class="inline-flex items-center justify-center gap-2 rounded border border-border px-6 py-3 text-sm font-medium text-text transition-all duration-200 hover:border-gold/30 hover:text-bright">
              See the Platform
            </a>
          </div>
        </div>
      </div>

      <!-- Compliance badges -->
      <div class="mt-16 flex flex-wrap gap-6" data-reveal data-reveal-delay="3">
        {['HIPAA Compliant', 'HITECH Ready', 'Zero Cloud Usage', 'Full Audit Trails'].map(text => (
          <div class="flex items-center gap-3">
            <div class="w-5 h-px bg-gold/30 shrink-0"></div>
            <span class="font-mono text-[10px] text-text/60 tracking-[0.2em] uppercase">{text}</span>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ===== PAIN POINTS ===== -->
  <section class="relative border-y border-border py-28 md:py-40 overflow-hidden">
    <div class="absolute inset-0 bg-radial-[at_70%_30%] from-danger/[0.015] to-transparent pointer-events-none"></div>

    <div class="mx-auto max-w-[90rem] px-6 md:px-10">
      <div class="grid md:grid-cols-12 gap-6 mb-20" data-reveal>
        <div class="md:col-span-7">
          <p class="font-mono text-[10px] text-danger/40 tracking-[0.3em] uppercase mb-6">The Problem</p>
          <h2 class="font-display text-4xl md:text-5xl tracking-[-0.02em] leading-[1.05]">The healthcare AI<br />dilemma<span class="text-danger/40">.</span></h2>
        </div>
        <div class="md:col-span-5 md:flex md:items-end">
          <p class="text-text text-[15px] leading-relaxed">
            AI can transform clinical workflows. But cloud AI creates compliance exposure that healthcare organizations cannot accept.
          </p>
        </div>
      </div>

      <div class="grid md:grid-cols-3 gap-x-16 lg:gap-x-24 gap-y-12">
        {painPoints.map((point, i) => (
          <div data-reveal data-reveal-delay={String(i + 1)}>
            <span class="font-mono text-[11px] text-danger/25 tracking-[0.15em]">{point.num}</span>
            <h3 class="font-display text-xl md:text-2xl text-bright mt-2 mb-3 tracking-tight">{point.title}</h3>
            <p class="text-text text-sm leading-relaxed">{point.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ===== USE CASES ===== -->
  <section class="py-28 md:py-40">
    <div class="mx-auto max-w-[90rem] px-6 md:px-10">
      <div class="grid md:grid-cols-12 gap-6 mb-20" data-reveal>
        <div class="md:col-span-7">
          <p class="font-mono text-[10px] text-gold/50 tracking-[0.3em] uppercase mb-6">Use Cases</p>
          <h2 class="font-display text-4xl md:text-5xl tracking-[-0.02em] leading-[1.05]">How healthcare organizations use AskSLM<span class="text-gold">.</span></h2>
        </div>
        <div class="md:col-span-5 md:flex md:items-end">
          <p class="text-text text-[15px] leading-relaxed">
            Domain-specific AI models trained on verified clinical literature, deployed entirely within your network.
          </p>
        </div>
      </div>

      <div class="space-y-0">
        {useCases.map((uc, i) => (
          <div class="border-t border-border py-10 md:py-12" data-reveal data-reveal-delay={String(i + 1)}>
            <div class="grid md:grid-cols-12 gap-4 md:gap-8 items-start">
              <span class="font-mono text-xs text-gold/40 md:col-span-1">0{i + 1}</span>
              <h3 class="md:col-span-4 font-display text-2xl md:text-3xl text-bright tracking-tight">{uc.title}</h3>
              <p class="md:col-span-7 text-text text-[15px] leading-relaxed">{uc.desc}</p>
            </div>
          </div>
        ))}
        <div class="border-t border-border"></div>
      </div>
    </div>
  </section>

  <!-- ===== SCENARIO ===== -->
  <section class="py-28 md:py-40">
    <div class="mx-auto max-w-[90rem] px-6 md:px-10">
      <div class="grid md:grid-cols-12 gap-12">
        <div class="md:col-span-5" data-reveal>
          <p class="font-mono text-[10px] text-gold/50 tracking-[0.3em] uppercase mb-6">In Practice</p>
          <h2 class="font-display text-4xl md:text-5xl tracking-[-0.02em] leading-[1.05]">What this looks like<span class="text-gold">.</span></h2>
        </div>
        <div class="md:col-span-7" data-reveal data-reveal-delay="1">
          <p class="text-text text-[15px] leading-relaxed mb-10">
            A healthcare provider integrates AskSLM into its hospital network. Clinicians use AI-powered tools to summarize patient histories before consultations, flag potential issues and lab anomalies, and securely query diagnostic models through encrypted on-premise connections -- all without sending a single byte of patient data outside their infrastructure.
          </p>
          <div class="grid sm:grid-cols-2 gap-4">
            {[
              'Summarizes patient histories before consultations',
              'Flags potential issues and lab anomalies',
              'Encrypted on-premise diagnostic queries',
              'Full compliance with healthcare privacy laws',
            ].map(outcome => (
              <div class="flex items-start gap-3">
                <div class="w-6 h-px bg-gold/40 mt-[11px] shrink-0"></div>
                <p class="text-text text-sm leading-relaxed">{outcome}</p>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== COMPLIANCE ===== -->
  <section class="border-t border-border py-28 md:py-40">
    <div class="mx-auto max-w-[90rem] px-6 md:px-10">
      <div class="grid md:grid-cols-12 gap-6 mb-20" data-reveal>
        <div class="md:col-span-7">
          <p class="font-mono text-[10px] text-gold/50 tracking-[0.3em] uppercase mb-6">Compliance</p>
          <h2 class="font-display text-4xl md:text-5xl tracking-[-0.02em] leading-[1.05]">Compliance is the architecture<span class="text-gold">.</span></h2>
        </div>
        <div class="md:col-span-5 md:flex md:items-end">
          <p class="text-text text-[15px] leading-relaxed">
            AskSLM was designed from day one for environments where data handling is not optional. Every design decision supports the compliance frameworks your organization operates under.
          </p>
        </div>
      </div>

      <div class="grid md:grid-cols-2 gap-x-16 lg:gap-x-24 gap-y-12">
        <div data-reveal data-reveal-delay="1">
          <span class="font-mono text-[11px] text-gold/25 tracking-[0.15em]">01</span>
          <h3 class="font-display text-xl md:text-2xl text-bright mt-2 mb-3 tracking-tight">HIPAA</h3>
          <p class="text-text text-sm leading-relaxed">
            Protected health information never leaves your infrastructure. Hardware-bound encryption, role-based access controls, and complete audit trails for every AI interaction.
          </p>
        </div>
        <div data-reveal data-reveal-delay="2">
          <span class="font-mono text-[11px] text-gold/25 tracking-[0.15em]">02</span>
          <h3 class="font-display text-xl md:text-2xl text-bright mt-2 mb-3 tracking-tight">HITECH</h3>
          <p class="text-text text-sm leading-relaxed">
            Full support for HITECH Act requirements including breach notification readiness, audit logging, and access controls aligned with meaningful use standards.
          </p>
        </div>
      </div>

      <div class="border-t border-border mt-16 pt-8" data-reveal>
        <a href="/security" class="font-mono text-[11px] text-gold/50 tracking-[0.2em] uppercase hover:text-gold transition-colors inline-flex items-center gap-2">
          See full security architecture
          <svg class="h-3.5 w-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" /></svg>
        </a>
      </div>
    </div>
  </section>

  <!-- ===== CTA ===== -->
  <section class="relative py-36 md:py-48 overflow-hidden">
    <div class="absolute inset-0 pointer-events-none">
      <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[60vw] h-[60vw] bg-radial-[at_50%_50%] from-gold/[0.025] to-transparent rounded-full"></div>
    </div>
    <div class="absolute top-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-gold/10 to-transparent"></div>

    <div class="relative mx-auto max-w-2xl px-6 text-center" data-reveal>
      <p class="font-mono text-[10px] text-gold/50 tracking-[0.3em] uppercase mb-10">Get Started</p>
      <h2 class="font-display text-5xl md:text-6xl tracking-[-0.03em] leading-[0.95]">
        See HIPAA-compliant<br />AI in action<span class="text-gold">.</span>
      </h2>
      <p class="mt-10 text-text text-lg max-w-md mx-auto leading-relaxed">
        We will walk you through a live deployment tailored to your hospital's infrastructure, compliance requirements, and clinical workflows.
      </p>
      <div class="mt-10 flex flex-wrap justify-center gap-4">
        <a href="/contact" class="group inline-flex items-center gap-2.5 rounded bg-gold px-8 py-3.5 text-sm font-semibold text-black transition-all duration-200 hover:bg-gold-bright hover:glow-gold-strong">
          Request a Healthcare Demo
          <svg class="h-4 w-4 transition-transform duration-200 group-hover:translate-x-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M17 8l4 4m0 0l-4 4m4-4H3" /></svg>
        </a>
      </div>
      <p class="mt-10 text-xs text-muted">No commitment. No data shared. 30-minute call.</p>
    </div>
  </section>

</Layout>

<script>
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add('revealed');
          observer.unobserve(entry.target);
        }
      });
    },
    { threshold: 0.1, rootMargin: '0px 0px -40px 0px' }
  );
  document.querySelectorAll('[data-reveal]').forEach((el) => observer.observe(el));
</script>
