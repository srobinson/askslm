---
import Layout from '../layouts/Layout.astro'
---

<Layout title="Security Architecture" description="AskSLM's security architecture ensures zero cloud exposure, hardware-bound encryption, full audit trails, and compliance with HIPAA, SOC 2, FISMA, and ISO 27001.">

  <!-- ===== HERO ===== -->
  <section class="relative min-h-[85svh] flex flex-col justify-end pb-16 md:justify-center md:pb-0 overflow-hidden">
    <div class="absolute inset-0 pointer-events-none">
      <div class="absolute top-[15%] left-[10%] w-[45vw] h-[45vw] bg-radial-[at_50%_50%] from-gold/[0.012] to-transparent rounded-full"></div>
      <div class="absolute bottom-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-gold/10 to-transparent"></div>
    </div>

    <div class="relative mx-auto w-full max-w-[90rem] px-6 md:px-10 pt-32 md:pt-0">
      <p class="font-mono text-[11px] text-gold/50 tracking-[0.3em] uppercase mb-10 md:mb-14" data-reveal>
        Security & Compliance
      </p>

      <div class="grid md:grid-cols-12 gap-8 md:gap-6">
        <div class="md:col-span-8">
          <h1 class="font-display leading-[0.85] tracking-[-0.04em]" data-reveal data-reveal-delay="1">
            <span class="block text-[clamp(3rem,9vw,8rem)] text-bright">Security is not</span>
            <span class="block text-[clamp(3rem,9vw,8rem)] text-bright/25 md:ml-[4vw]">a feature</span>
            <span class="block text-[clamp(3rem,9vw,8rem)] text-bright">we added<span class="text-gold">.</span></span>
          </h1>
        </div>

        <div class="md:col-span-4 md:flex md:flex-col md:justify-end md:pb-4" data-reveal data-reveal-delay="2">
          <p class="font-display italic text-xl md:text-2xl text-bright/70 leading-snug mb-4">
            It is the architecture.
          </p>
          <p class="text-[15px] text-text leading-relaxed max-w-sm">
            Every design decision in AskSLM -- from hardware-bound encryption to model provenance tracking -- exists because regulated industries demand it. Compliance is not a checkbox. It is the reason we exist.
          </p>
          <div class="mt-8 flex flex-col sm:flex-row md:flex-col gap-3">
            <a href="/contact" class="group inline-flex items-center justify-center gap-2.5 rounded bg-gold px-6 py-3 text-sm font-semibold text-black transition-all duration-200 hover:bg-gold-bright">
              Schedule a Security Review
              <svg class="h-4 w-4 transition-transform duration-200 group-hover:translate-x-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M17 8l4 4m0 0l-4 4m4-4H3" /></svg>
            </a>
            <a href="/platform" class="inline-flex items-center justify-center gap-2 rounded border border-border px-6 py-3 text-sm font-medium text-text transition-all duration-200 hover:border-gold/30 hover:text-bright">
              Platform Overview
            </a>
          </div>
        </div>
      </div>

      <!-- Security pillars strip -->
      <div class="mt-16 flex flex-wrap gap-x-8 gap-y-3" data-reveal data-reveal-delay="3">
        {['Zero Cloud Exposure', 'Hardware-Bound Encryption', 'Full Audit Trails', 'Role-Based Access', 'Model Provenance'].map(text => (
          <div class="flex items-center gap-3">
            <div class="w-1.5 h-1.5 rounded-full bg-gold/40"></div>
            <span class="text-[11px] font-mono text-muted tracking-[0.15em] uppercase">{text}</span>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ===== SECTION 1: SECURITY ARCHITECTURE OVERVIEW ===== -->
  <section class="border-t border-border py-28 md:py-40">
    <div class="mx-auto max-w-[90rem] px-6 md:px-10">
      <div class="grid lg:grid-cols-12 gap-16 items-start">
        <div class="lg:col-span-7">
          <div data-reveal>
            <p class="font-mono text-[10px] text-gold/50 tracking-[0.3em] uppercase mb-6">Architecture</p>
            <h2 class="font-display text-4xl md:text-5xl tracking-[-0.02em]">Security architecture overview<span class="text-gold">.</span></h2>
            <p class="mt-6 text-text leading-relaxed max-w-2xl">
              AskSLM processes everything locally. Your data never crosses a network boundary you don't control. There is no cloud component, no external API dependency, and no mode in which sensitive data leaves your infrastructure.
            </p>
          </div>

          <div class="mt-14 space-y-0">
            {[
              {
                num: '01',
                title: 'Local-only data flow',
                desc: 'Every query, every response, and every training job runs within your infrastructure perimeter. There is no "hybrid cloud" fallback. The architecture is physically incapable of sending data externally during inference.',
              },
              {
                num: '02',
                title: 'Hardware-bound encryption',
                desc: 'Trusted Execution Environments (TEEs) provide hardware-level encryption. Even if someone physically accesses a server, models and data remain cryptographically locked to the authorized hardware.',
              },
              {
                num: '03',
                title: 'Network isolation by design',
                desc: 'The inference engine requires no outbound network connections during operation. Air-gapped deployment is fully supported for the most sensitive environments.',
              },
            ].map((item, i) => (
              <div class="border-t border-border py-8" data-reveal data-reveal-delay={String(i + 1)}>
                <span class="font-mono text-[11px] text-gold/30 tracking-[0.15em]">{item.num}</span>
                <h3 class="font-display text-xl text-bright mt-2 mb-3 tracking-tight">{item.title}</h3>
                <p class="text-sm text-text leading-relaxed max-w-lg">{item.desc}</p>
              </div>
            ))}
          </div>
        </div>

        <!-- Data flow visualization -->
        <div class="lg:col-span-5 lg:mt-20" data-reveal data-reveal-delay="2">
          <div class="border-t border-border pt-8">
            <p class="font-mono text-[10px] text-gold/50 tracking-[0.3em] uppercase mb-8">What Stays Local</p>
            <div class="space-y-0">
              {[
                'Queries & responses',
                'Training data',
                'Model weights',
                'Audit logs',
                'User credentials',
                'Document embeddings',
                'RAG knowledge base',
                'API keys & tokens',
              ].map(label => (
                <div class="flex items-center justify-between border-b border-border py-3.5">
                  <span class="text-sm text-text">{label}</span>
                  <span class="inline-flex items-center gap-2 text-[10px] font-mono text-gold/60 tracking-[0.1em] uppercase">
                    <div class="w-1.5 h-1.5 rounded-full bg-gold/40"></div>
                    On-premise only
                  </span>
                </div>
              ))}
            </div>
            <div class="border-b border-border py-6 text-center">
              <span class="text-sm text-muted">Data sent to external cloud</span>
              <span class="block font-display text-3xl text-bright mt-2">0<span class="text-gold"> bytes</span></span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== SECTION 2: DATA PROTECTION ===== -->
  <section class="border-t border-border py-28 md:py-40">
    <div class="mx-auto max-w-[90rem] px-6 md:px-10">
      <div class="mb-20" data-reveal>
        <p class="font-mono text-[10px] text-gold/50 tracking-[0.3em] uppercase mb-6">Data Protection</p>
        <div class="grid md:grid-cols-12 gap-6">
          <div class="md:col-span-7">
            <h2 class="font-display text-4xl md:text-5xl tracking-[-0.02em]">Multilayered protection<span class="text-gold">.</span></h2>
          </div>
          <div class="md:col-span-5 md:flex md:items-end">
            <p class="text-text text-[15px] leading-relaxed">
              Every byte accounted for. Every access controlled.
            </p>
          </div>
        </div>
      </div>

      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-x-16 lg:gap-x-24 gap-y-12 md:gap-y-16">
        {[
          {
            num: '01',
            title: 'Zero cloud usage',
            desc: 'No data is sent to third-party clouds during inference, training, or any other operation. This is not a setting you enable -- it is a structural impossibility.',
          },
          {
            num: '02',
            title: 'On-site processing',
            desc: 'All inference, training, and data retrieval occurs within your hardware or SLM appliance. Your data paths are fully auditable and physically contained.',
          },
          {
            num: '03',
            title: 'Encryption at rest and in transit',
            desc: 'Data encrypted at rest with hardware-bound keys. All inter-component communication uses TLS. Trusted Execution Environments provide an additional hardware encryption layer.',
          },
          {
            num: '04',
            title: 'Role-based access control',
            desc: 'Granular RBAC ensures only authorized users and systems interact with specific models and data. Define roles by department, clearance level, or use case.',
          },
          {
            num: '05',
            title: 'Segregated storage',
            desc: 'Model weights and user data are stored in separate, isolated environments. Cross-environment leakage is architecturally prevented at the storage layer.',
          },
          {
            num: '06',
            title: 'Policy enforcement',
            desc: 'Automated policy enforcement blocks unauthorized data flows and access patterns. Policies are defined centrally and applied consistently across the platform.',
          },
        ].map((item, i) => (
          <div data-reveal data-reveal-delay={String((i % 3) + 1)}>
            <span class="font-mono text-[11px] text-gold/25 tracking-[0.15em]">{item.num}</span>
            <h3 class="font-display text-xl text-bright mt-2 mb-3 tracking-tight">{item.title}</h3>
            <p class="text-sm text-text leading-relaxed">{item.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ===== SECTION 3: COMPLIANCE FRAMEWORKS ===== -->
  <section class="border-t border-border py-28 md:py-40">
    <div class="mx-auto max-w-[90rem] px-6 md:px-10">
      <div class="mb-20" data-reveal>
        <p class="font-mono text-[10px] text-gold/50 tracking-[0.3em] uppercase mb-6">Compliance</p>
        <h2 class="font-display text-4xl md:text-5xl tracking-[-0.02em]">Built for the frameworks that govern your industry<span class="text-gold">.</span></h2>
      </div>

      <div class="space-y-0">
        {[
          {
            framework: 'HIPAA',
            status: 'Architecture aligned',
            statusActive: true,
            industry: 'Healthcare',
            details: [
              'PHI never leaves your infrastructure',
              'Full audit trails for every AI interaction',
              'BAA-ready architecture',
              'Segregated data storage prevents cross-contamination',
            ],
          },
          {
            framework: 'SOC 2',
            status: 'In progress',
            statusActive: true,
            industry: 'Enterprise',
            details: [
              'Trust service criteria alignment',
              'Continuous monitoring and logging',
              'Access controls and authentication',
              'Incident response procedures',
            ],
          },
          {
            framework: 'FISMA',
            status: 'Architecture aligned',
            statusActive: true,
            industry: 'Government',
            details: [
              'Air-gapped deployment supported',
              'NIST 800-53 control mapping',
              'Continuous monitoring capabilities',
              'Authority to Operate documentation support',
            ],
          },
          {
            framework: 'ISO 27001',
            status: 'Roadmap',
            statusActive: false,
            industry: 'International',
            details: [
              'Information security management system alignment',
              'Risk assessment and treatment processes',
              'Asset management and access control',
              'Operational security procedures',
            ],
          },
        ].map((card, i) => (
          <div class="border-t border-border py-10 md:py-14" data-reveal data-reveal-delay={String(i + 1)}>
            <div class="grid md:grid-cols-12 gap-6 md:gap-8 items-start">
              <!-- Framework name + status -->
              <div class="md:col-span-3">
                <div class="flex items-center gap-4 md:flex-col md:items-start md:gap-2">
                  <h3 class="font-display text-3xl md:text-4xl text-bright tracking-tight">{card.framework}</h3>
                  <span class={`inline-flex items-center gap-2 font-mono text-[9px] font-medium tracking-[0.2em] uppercase ${card.statusActive ? 'text-gold/60' : 'text-muted/50'}`}>
                    <span class={`h-1.5 w-1.5 rounded-full ${card.statusActive ? 'bg-gold/50' : 'bg-muted/30'}`}></span>
                    {card.status}
                  </span>
                </div>
                <p class="font-mono text-[10px] text-muted/40 tracking-[0.15em] uppercase mt-2">{card.industry}</p>
              </div>

              <!-- Details -->
              <div class="md:col-span-9">
                <div class="grid sm:grid-cols-2 gap-x-12 gap-y-3">
                  {card.details.map(detail => (
                    <div class="flex items-start gap-3">
                      <div class="w-4 h-px bg-gold/30 mt-[10px] shrink-0"></div>
                      <span class="text-sm text-text leading-relaxed">{detail}</span>
                    </div>
                  ))}
                </div>
              </div>
            </div>
          </div>
        ))}
        <div class="border-t border-border"></div>
      </div>

      <div class="mt-12 max-w-2xl" data-reveal>
        <p class="text-sm text-muted leading-relaxed">
          Compliance status is presented honestly. "Architecture aligned" means the technical architecture supports the framework's requirements. Formal certification timelines available upon request.
        </p>
      </div>
    </div>
  </section>

  <!-- ===== SECTION 4: MODEL INTEGRITY ===== -->
  <section class="border-t border-border py-28 md:py-40">
    <div class="mx-auto max-w-[90rem] px-6 md:px-10">
      <div class="grid lg:grid-cols-12 gap-16 items-start">
        <div class="lg:col-span-7">
          <div data-reveal>
            <p class="font-mono text-[10px] text-gold/50 tracking-[0.3em] uppercase mb-6">Model Integrity</p>
            <h2 class="font-display text-4xl md:text-5xl tracking-[-0.02em]">Provenance you can verify<span class="text-gold">.</span></h2>
            <p class="mt-6 text-text leading-relaxed max-w-2xl">
              Every model running on your infrastructure is verified, versioned, and traceable. You know exactly what trained it, who built it, and when it changed.
            </p>
          </div>

          <div class="mt-14 space-y-0">
            {[
              {
                num: '01',
                title: 'Cryptographic signing',
                desc: 'Every model build is cryptographically signed. The inference engine verifies signatures before execution, ensuring only authorized, unmodified models run on your hardware.',
              },
              {
                num: '02',
                title: 'Version tracking and lineage',
                desc: 'Complete version history for every model. Track every update, every dataset change, and every configuration modification with full lineage documentation.',
              },
              {
                num: '03',
                title: 'Training data provenance',
                desc: 'Transparent documentation of training sources for every model. Know exactly what data informed the model\'s behavior -- critical for regulatory audits and liability management.',
              },
              {
                num: '04',
                title: 'Integrity checks before execution',
                desc: 'Models undergo integrity verification before they process any data. Tampered or unverified models are rejected automatically at the engine level.',
              },
            ].map((item, i) => (
              <div class="border-t border-border py-8" data-reveal data-reveal-delay={String(i + 1)}>
                <span class="font-mono text-[11px] text-gold/30 tracking-[0.15em]">{item.num}</span>
                <h3 class="font-display text-xl text-bright mt-2 mb-3 tracking-tight">{item.title}</h3>
                <p class="text-sm text-text leading-relaxed max-w-lg">{item.desc}</p>
              </div>
            ))}
          </div>
        </div>

        <!-- Model lifecycle visual -->
        <div class="lg:col-span-5 lg:mt-20" data-reveal data-reveal-delay="2">
          <div class="border-t border-border pt-8">
            <p class="font-mono text-[10px] text-gold/50 tracking-[0.3em] uppercase mb-10">Model Lifecycle Security</p>

            <div class="relative">
              <!-- Vertical gold line -->
              <div class="absolute left-[7px] top-2 bottom-2 w-px bg-gradient-to-b from-gold/20 via-gold/10 to-transparent"></div>

              <div class="space-y-10">
                {[
                  { step: '01', label: 'Build', desc: 'Model trained on verified, documented data sources by authorized vendor' },
                  { step: '02', label: 'Sign', desc: 'Cryptographic signature applied to model build artifact' },
                  { step: '03', label: 'Verify', desc: 'Integrity check validates signature and provenance before deployment' },
                  { step: '04', label: 'Deploy', desc: 'Model deployed to your engine via encrypted pipeline' },
                  { step: '05', label: 'Monitor', desc: 'Continuous version tracking, access logging, and anomaly detection' },
                  { step: '06', label: 'Audit', desc: 'Full trail available for compliance review at any time' },
                ].map((item, i) => (
                  <div class="relative pl-10" data-reveal data-reveal-delay={String(Math.min(i + 1, 6))}>
                    <div class="absolute left-0 top-1.5 h-[15px] w-[15px] rounded-full border border-gold/25 bg-black flex items-center justify-center">
                      <div class="h-[5px] w-[5px] rounded-full bg-gold/50"></div>
                    </div>
                    <span class="font-mono text-[10px] text-gold/40 tracking-[0.15em]">{item.step}</span>
                    <h4 class="font-display text-lg text-bright mt-1 mb-1">{item.label}</h4>
                    <p class="text-xs text-text leading-relaxed">{item.desc}</p>
                  </div>
                ))}
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== SECTION 5: AUDIT & GOVERNANCE ===== -->
  <section class="border-t border-border py-28 md:py-40">
    <div class="mx-auto max-w-[90rem] px-6 md:px-10">
      <div class="mb-20" data-reveal>
        <p class="font-mono text-[10px] text-gold/50 tracking-[0.3em] uppercase mb-6">Governance</p>
        <div class="grid md:grid-cols-12 gap-6">
          <div class="md:col-span-7">
            <h2 class="font-display text-4xl md:text-5xl tracking-[-0.02em]">Audit & governance<span class="text-gold">.</span></h2>
          </div>
          <div class="md:col-span-5 md:flex md:items-end">
            <p class="text-text text-[15px] leading-relaxed">
              Complete visibility into every AI interaction. The audit trail your compliance team requires.
            </p>
          </div>
        </div>
      </div>

      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-x-16 lg:gap-x-20 gap-y-12 md:gap-y-16">
        {[
          {
            num: '01',
            title: 'Audit trails',
            desc: 'Every model interaction logged with timestamp, user, query hash, model version, and response metadata. Exportable for external compliance review.',
          },
          {
            num: '02',
            title: 'Logging & monitoring',
            desc: 'Real-time monitoring of system health, model performance, and access patterns. Alerts on anomalous behavior or policy violations.',
          },
          {
            num: '03',
            title: 'Policy enforcement',
            desc: 'Define and enforce data handling policies centrally. Automated checks ensure workloads align with internal and regulatory requirements.',
          },
          {
            num: '04',
            title: 'Incident readiness',
            desc: 'Documented incident response procedures. Threat detection and anomaly monitoring. Vulnerability scanning and environment hardening.',
          },
        ].map((item, i) => (
          <div data-reveal data-reveal-delay={String(i + 1)}>
            <span class="font-mono text-[11px] text-gold/25 tracking-[0.15em]">{item.num}</span>
            <h3 class="font-display text-xl text-bright mt-2 mb-3 tracking-tight">{item.title}</h3>
            <p class="text-sm text-text leading-relaxed">{item.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ===== CTA ===== -->
  <section class="relative py-36 md:py-48 overflow-hidden">
    <div class="absolute inset-0 pointer-events-none">
      <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[60vw] h-[60vw] bg-radial-[at_50%_50%] from-gold/[0.025] to-transparent rounded-full"></div>
    </div>
    <div class="absolute top-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-gold/10 to-transparent"></div>

    <div class="relative mx-auto max-w-2xl px-6 text-center" data-reveal>
      <p class="font-mono text-[10px] text-gold/50 tracking-[0.3em] uppercase mb-10">Talk to Us</p>
      <h2 class="font-display text-5xl md:text-6xl lg:text-7xl tracking-[-0.03em] leading-[0.95]">
        Have security<br />questions<span class="text-gold">?</span>
      </h2>
      <p class="mt-10 text-text text-lg max-w-xl mx-auto leading-relaxed">
        We'll walk through our security architecture in detail, answer your compliance questions, and discuss how AskSLM meets your specific regulatory requirements.
      </p>
      <div class="mt-10 flex flex-wrap justify-center gap-4">
        <a href="/contact" class="group inline-flex items-center gap-2.5 rounded bg-gold px-8 py-3.5 text-sm font-semibold text-black transition-all duration-200 hover:bg-gold-bright hover:glow-gold-strong">
          Schedule a Security Review
          <svg class="h-4 w-4 transition-transform duration-200 group-hover:translate-x-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M17 8l4 4m0 0l-4 4m4-4H3" /></svg>
        </a>
        <a href="/pricing" class="inline-flex items-center gap-2 rounded border border-border px-8 py-3.5 text-sm font-medium text-text transition-all duration-200 hover:border-gold/30 hover:text-bright">
          View Pricing
        </a>
      </div>
      <p class="mt-10 text-xs text-muted">No commitment. No data shared. We're happy to sign an NDA before the call.</p>
    </div>
  </section>

</Layout>

<script>
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add('revealed');
          observer.unobserve(entry.target);
        }
      });
    },
    { threshold: 0.1, rootMargin: '0px 0px -40px 0px' }
  );
  document.querySelectorAll('[data-reveal]').forEach((el) => observer.observe(el));
</script>
